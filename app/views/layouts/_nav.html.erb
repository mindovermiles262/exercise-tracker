<nav class="navbar navbar-light bg-primary">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to "Exercise Tracker", root_url, id: 'brand', class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if logged_in? %>
      <!-- Navbar if logged in:  -->
        <ul class="nav navbar-nav navbar-center">
          <li><a id="link">Messages</a></li>
          <li><%= link_to "Leaderboard", users_path, id: "link" %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><a class="emphasize btn btn-success" id="">Enter Activity</a></li>
          <% if current_user %>
            <li><%= link_to current_user.name, current_user, id: "link" %></li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle bg-primary" id="link" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-cog icon" aria-hidden="true"></i></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
              <li><a href="#">Your Calendar</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Log Out", logout_path %></li>
            </ul>
          </li>
        </ul>
      <% else %>  
      <!-- Navbar as guest -->
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Sign Up Now", signup_path, id: "link", class: 'border-white-1' %></li>
          <li><%= link_to "Log In", login_path, id: "link" %></li>
        </ul>
      <% end %>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>